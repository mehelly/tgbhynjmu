apiVersion: v1
kind: Service
metadata:
  name: fullre
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "fullre"
    port: 8080
  selector:
    app: fullre
