apiVersion: v1
kind: Service
metadata:
  name: mentyuhu
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "mentyuhu"
    port: 8080
  selector:
    app: mentyuhu
